@model Beckaroo_NetCore.Models.Blog

@{
    ViewData["Title"] = @Model.Title;
}

<div class="container BodyContainer">
    <div class="col-10 text-center offset-1">
        <h1>@Model.Title</h1>
        <h4>Author: @Model.Author</h3>
        <p class="BlogDate">@Html.DisplayFor(m => m.PublishDate)</p>
        @if (Model.Image != null)
        {
            <img class="StandardImage" src="~/images/@Model.Image" alt="@Model.ImageAlt">
        }
        <div class="BlogBody text-justify">
            @Html.Raw(Model.Content)
        </div>
    </div>
</div>